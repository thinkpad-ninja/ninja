# Only display the OSD on the currently focused monitor
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

# Laptop multimedia keys for volume and LCD brightness (with OSD)
bindeld = ,XF86AudioRaiseVolume, Volume up, exec, $osdclient --output-volume +15
bindeld = ,XF86AudioLowerVolume, Volume down, exec, $osdclient --output-volume -15
bindeld = ,XF86AudioMute, Mute, exec, $osdclient --output-volume mute-toggle
bindeld = ,XF86AudioMicMute, Mute microphone, exec, $osdclient --input-volume mute-toggle
bindeld = ,XF86MonBrightnessUp, Brightness up, exec, $osdclient --brightness +20
bindeld = ,XF86MonBrightnessDown, Brightness down, exec, $osdclient --brightness -20

# Precise 1% multimedia adjustments with Alt modifier
bindeld = ALT, XF86AudioRaiseVolume, Volume up precise, exec, $osdclient --output-volume +1
bindeld = ALT, XF86AudioLowerVolume, Volume down precise, exec, $osdclient --output-volume -1
bindeld = ALT, XF86MonBrightnessUp, Brightness up precise, exec, $osdclient --brightness +1
bindeld = ALT, XF86MonBrightnessDown, Brightness down precise, exec, $osdclient --brightness -1

bindeld = ALT, J, Brightness down, exec, $osdclient --brightness -20
bindeld = ALT, K, Brightness up, exec, $osdclient --brightness +20


bindeld = ALT, A, Volume down, exec, $osdclient --output-volume -15
bindeld = ALT, S, Volume up, exec, $osdclient --output-volume +15
bindeld = SUPER SHIFT, A, Mute, exec, $osdclient --output-volume mute-toggle

bindeld = ALT, W, Brightness 12, exec, $osdclient --brightness 16
bindeld = ALT, E, Brightness 96, exec, $osdclient --brightness 95
bindeld = SUPER SHIFT, E, Brightness dim/off, exec, ~/.config/hypr/scripts/brightness-toggle-dim.sh

bindeld = SUPER SHIFT, R, Volume cycle, exec, $osdclient --output-volume mute-toggle
#
# Requires playerctl
bindld = , XF86AudioNext, Next track, exec, $osdclient --playerctl next
bindld = , XF86AudioPause, Pause, exec, $osdclient --playerctl play-pause
bindld = , XF86AudioPlay, Play, exec, $osdclient --playerctl play-pause
bindld = , XF86AudioPrev, Previous track, exec, $osdclient --playerctl previous

# Switch audio output with Super + Mute
bindld = SUPER, XF86AudioMute, Switch audio output, exec, omarchy-cmd-audio-switch
